[default]
inventory = ./inventory.ini # This line specifies the location of the inventory file that Ansible will use to determine which hosts to manage. In this case, it is set to "./inventory.ini", which means that Ansible will look for an inventory file named "inventory.ini" in the current directory.
log_path = /var/log/roboshop/ansible.log
